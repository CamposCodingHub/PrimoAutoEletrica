# P0.02 / P0.11 / P0.12 — status (2026-09-20)

## P0.02 API auth
STATUS: **FEITO na branch `audit/product-discovery-2026-09`** (nao em `main` ainda).
EVIDENCIA HTTP real (WebApplicationFactory):
- sem token → **401** (orcamentos/estoque/financeiro)
- health sem token → **200**
- token invalido → **401**
- token sem claim ORCAMENTO_LER → **403**
- token dev com perms → nao 401/403
Testes: `ApiJwtHttpTests` (7) + gates fonte.

## P0.11 Backup encrypt
STATUS: **FEITO (honestidade)** — feature desabilitada; CreateZipAsync com senha lanca NotSupportedException (nao cria ZIP falso-protegido).
UI: checkbox "Criptografar backup (indisponivel)" IsEnabled=False.
Teste: `ExternalBackupEncryptionHonestyTests` PASS.

## P0.12 Cloud backup
STATUS: **FEITO (honestidade)** — Sync cloud + ComboBox provedores desabilitados; SyncWithCloud forçado false.
Nao ha OAuth/upload real.
