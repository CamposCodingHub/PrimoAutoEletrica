# P0.06 — Contratos API (auth)

STATUS: **FEITO (auth contracts + testes HTTP)** no branch `audit/product-discovery-2026-09`
STATUS main: **NÃO FEITO** (sem JWT)

## Endpoints

| Método | Path | Auth |
|--------|------|------|
| GET | `/api/health` | Anônimo (200) |
| POST | `/api/auth/token` | Anônimo (dev token se configurado) |
| GET/POST/PUT/DELETE | `/api/orcamentos*` | `ORCAMENTO_*` |
| GET | `/api/estoque/produtos*` | `ESTOQUE_LER` |
| GET/POST | `/api/financeiro*` | `FINANCEIRO_*` |

## Contratos de erro

- Sem Bearer / token inválido → **401**
- Token válido sem policy claim → **403**
- Exceção de negócio → `SafeProblem` (mensagem pública; **sem** `ex.Message` interno)

## Evidência

- CODE: `PrimoAutoEletrica.Api/Program.cs` (`AddJwtBearer`, `UseAuthentication`, policies)
- TEST: `ApiJwtHttpTests` (live WebApplicationFactory) — no-token 401, invalid 401, health 200, limited claims 403
- Suite security filter (2026-09-20): **56 PASS** (Permission/JWT/License/Backup/360/Historico)
