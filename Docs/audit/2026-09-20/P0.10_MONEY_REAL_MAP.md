# P0.10 — Dinheiro / SQLite REAL

STATUS: **MAPEADO — migracao INTEGER cents NAO FEITA nesta rodada**

Politica alvo: INTEGER centavos (ou DECIMAL em SQL Server/Postgres futuro).

Inventario (amostra): PrecoCompra, PrecoVenda, Total, Desconto, valores financeiro — hoje tipicamente REAL/decimal double no SQLite.

Proximo passo controlado:
1. schema dual-write cents
2. testes arredondamento/parcelas/desconto
3. migracao dados
4. cutover leitura

Nao migrar cego sem plano de dados.
