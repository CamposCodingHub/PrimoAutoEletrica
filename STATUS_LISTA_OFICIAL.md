# Status da Lista Oficial - Primo Auto Eletrica

Atualizado em `2026-05-25`.

Este arquivo resume o estado atual da ordem oficial de execucao da lista.
Nao marca nada como `100% concluido` sem evidencia funcional real.

## Legenda

- `Concluido`: entregue com evidencia funcional e cobertura automatizada suficiente
- `Em andamento`: existe base implementada e validacao parcial real
- `Parcial`: ha estrutura pronta, mas ainda faltam testes funcionais ou cobertura total
- `Pendente`: ainda nao foi trabalhado no nivel necessario para uso real

## Ordem oficial de execucao

| Ordem | Item | Status | Evidencia atual | Proximo passo recomendado |
|---|---|---|---|---|
| 1 | corrigir erros e telas fechando | Concluido | `build` com `0 erros/0 avisos`, smoke test UI com `128/128` checks e workflow test com `38/38` checks; cadastros principais e repositorios compartilham validacao central de `CPF/CNPJ`, telefone, e-mail, placa, datas e duplicidade, os fluxos de `PDV`, `OrdensServico`, `Orcamentos`, `Financeiro` e `Agendamentos` barram descontos/totais/estoque/datas inconsistentes, as exclusoes/cancelamentos criticos exigem confirmacao digitada, e a rodada final confirmou estabilidade inclusive na simulacao completa de botoes e janelas sem travar a aplicacao | manter a cobertura automatizada e monitorar regressao nas frentes seguintes |
| 2 | estabilizar navegacao | Concluido | `MainWindow` valida sequencia, retorno, refresh seguro, busca global sob demanda, abertura automatizada de NF-e, notificacoes acionaveis no shell, logs contextuais de navegacao e acoes cruzadas partindo de `Agendamentos` e `Orcamentos` para modulos relacionados; o smoke test cobre esse pacote dentro de uma rodada UI `128/128` aprovada | manter o shell desacoplado e reaproveitar a malha de notificacoes/navegacao nas proximas frentes |
| 3 | padronizar visual global | Concluido | `Modal.xaml` consolidou a base reutilizavel e toda a malha de `Views` continua consumindo o design system global, incluindo `OperacaoCaixaWindow`, `OrcamentosDashboardControl`, `SelecionarVendaWindow` e `HistoricoEstoqueWindow`; a varredura local nao encontrou `Views` fora do padrao e o smoke test validou o conjunto com `128/128` checks aprovados | preservar o design system como base das proximas telas e deixar o polimento final para a etapa dedicada |
| 4 | corrigir performance inicial | Concluido | `Relatorios` carrega datasets pesados em background com loading visual, filtros no SQL e `LIMIT` real por bloco, a auditoria operacional usa paginacao dedicada, o `FinanceiroViewModel` consulta apenas o periodo necessario, e a camada de migracao criou indices adicionais para relatorios, auditoria e financas; a validacao automatizada fechou em smoke UI `128/128` e workflow `38/38` | manter a telemetria de duracao por modulo e reaplicar o padrao assincrono nas proximas telas pesadas |
| 5 | melhorar logs | Concluido | `LoggerService` grava log global e logs por area em `Logs/Areas`, as consultas criticas de `Relatorios`, `Auditoria` e `Financeiro` medem e registram duracao por modulo, `Relatorios` ganhou consulta operacional dedicada com filtro por area, severidade, status, usuario e termo livre, e o `PDV` agora registra diagnostico detalhado de impressoras instaladas, default, driver, porta e fila virtual/fisica; smoke UI `128/128` e workflow `38/38` validam a malha atual | continuar expandindo a taxonomia de areas conforme novas frentes operacionais surgirem |
| 6 | melhorar permissoes | Concluido | a malha de permissoes por acao foi fechada nos modulos restantes: `Clientes`, `Veiculos`, `Fornecedores`, `Orcamentos`, `Agendamentos`, `Estoque`, `PDV`, `OrdensServico`, `Financeiro` e administracao validam criacao, edicao, duplicacao, compartilhamento, check-in/check-out, inventario e cancelamentos criticos por codigo granular, com fallback por perfil e confirmacao critica nas operacoes sensiveis; o workflow test valida essa camada com `Seguranca:PermissoesGranularesOperacionais` dentro de uma rodada `38/38` | manter a cobertura automatizada e expandir novos codigos granulares conforme surgirem frentes novas |
| 7 | corrigir PDV | Parcial | o `PDV` exige caixa aberto para vender, abre/fecha caixa, registra sangria e suprimento, integra a venda ao financeiro e ao caixa operacional, trabalha com historico operacional de vendas recentes para reimpressao e cancelamento completo, respeita disponibilidade operacional de estoque descontando reservas, e agora registra diagnostico operacional da infraestrutura de impressao e da impressora escolhida na reimpressao; o workflow test valida `PDV:HistoricoOperacional`, `PDV:DiagnosticoImpressao` e `PDV:CancelarVendaHistorico` dentro de `38/38`; a evidencia ainda ausente continua sendo a reimpressao em impressora fisica real, e a verificacao local de `2026-05-25` confirmou que este ambiente so disponibiliza `Microsoft Print to PDF` | validar a reimpressao em uma impressora fisica real no ambiente de operacao |
| 8 | corrigir estoque | Concluido | o estoque fecha o ciclo operacional completo: reserva de pecas no check-in/conversao de agendamento em `OS`, consumo da reserva na entrega, contagem de inventario com reconciliacao real, historico operacional por produto e grade principal exibindo `reservado/disponivel`; o workflow test valida `Estoque:ValidarReservaAgendamento`, `Estoque:ReservaConsumidaNaOS`, `Estoque:ValidarBaixaOS` e `Estoque:InventarioEHistorico` dentro de `38/38` checks aprovados | manter a telemetria da malha operacional e evoluir analytics avancados como curva ABC e custo medio |
| 9 | corrigir financeiro | Concluido | o `Financeiro` agora fecha contas, baixas e DRE no fluxo real: a tela opera com selecao direta de contas, baixas operacionais e demonstrativo consolidado, a conversao de `Orcamentos` em venda integra conta a receber e movimentacao idempotentes, e o workflow test cobre `Financeiro:BaixasEDre` e `Orcamentos:ConverterEmVendaFinanceiro` dentro de `38/38` checks aprovados | manter a malha automatizada e evoluir analytics financeiros mais avancados em frentes futuras |
| 10 | corrigir OS | Concluido | `OS` agora persiste e reapresenta `checklist`, observacoes de garantia e validade, exporta esse bloco no painel operacional/impressao/PDF e continua fechando estoque e financeiro na entrega; o workflow test valida `OS:Criar`, `OS:ChecklistGarantiaPainel` e a finalizacao operacional dentro de `38/38` checks aprovados | manter a base e tratar anexos/fotos como evolucao futura |
| 11 | corrigir agendamentos | Concluido | o servico passou a recarregar timeline completa do banco, os alertas cobrem atraso, execucao prolongada e veiculo pronto, e o fluxo diario segue integrado com `OS`, estoque e financeiro; o workflow test agora valida `Agendamentos:TimelineEAlertas` alem de conversao e checkout, totalizando `38/38` checks aprovados | reaproveitar essa trilha operacional nas futuras melhorias de UX da agenda |
| 12 | corrigir orcamentos | Concluido | `Orcamentos` agora fecha aprovacao comercial, geracao de PDF e dupla conversao operacional: em venda com reflexo financeiro/estoque e em `OS` com vinculo persistido; o workflow test valida `Orcamentos:AprovarEPdf`, `Orcamentos:ConverterEmVendaFinanceiro` e `Orcamentos:ConverterEmOS` dentro de `38/38` checks aprovados | manter os status oficiais estaveis e expandir apenas regras comerciais futuras |
| 13 | corrigir relatorios | Concluido | `Relatorios` agora expoe resumo de consistencia operacional entre modulos, suporta exportacao operacional validada e ganhou cobertura dedicada em smoke e workflow; a rodada final aprovou `Relatorios:ConsistenciaOperacional` no smoke e `Relatorios:ConsistenciaEExportacoes` no workflow, com `128/128` e `38/38` checks | continuar evoluindo indicadores executivos sem perder a consistencia operacional validada |
| 14 | implementar backup | Concluido | backup agora profissional com nomes padronizados `PrimoAutoEletrica_Backup_YYYY-MM-DD_HH-mm-ss.db` e manifesto JSON contendo hash SHA256, versao do sistema, usuario e ambiente; implementados `CriarBackupAntesAtualizacao` e `CriarBackupAntesMigracao` com bloqueio se falhar; restauracao segura cria backup de seguranca antes; alertas operacionais via `ShellNotificationService` quando backup falha; suporte a backup em rede/HD externo configuravel na tela; aba dedicada de Backup na tela Configuracoes do Sistema com informacoes do ultimo backup e teste de conexao de rede; workflow test valida `Backup:CriarEVerificar`, `Backup:AntesAtualizacao`, `Backup:AntesMigracao` e `Backup:NomePadronizado` dentro de `42/42` checks aprovados | manter a malha automatizada e evoluir para integracao com nuvem em frentes futuras |
| 15 | preparar SQL Server | Parcial | configuracao e plano de provider foram iniciados, e a nova tela base de configuracoes ja persiste timeout de sessao e timeout tecnico do banco por interface | implementar provider real, tela e teste de conexao |
| 16 | preparar multiusuario | Parcial | workflow test valida bloqueio de registro entre duas sessoes sinteticas | adicionar sincronizacao e concorrencia real entre estacoes |
| 17 | testar rede local | Pendente | ainda sem validacao real entre computadores | montar cenario de duas estacoes com banco central |
| 18 | testar fluxo completo | Pendente | ainda sem execucao completa do roteiro de uso real | rodar roteiro operacional item a item com evidencias |
| 19 | polir visual final | Pendente | propositalmente nao priorizado antes da base operacional | voltar nisso depois da estabilizacao funcional |
| 20 | preparar versao de uso real | Pendente | ainda faltam rede, fluxo completo e implantacao | consolidar instalacao, configuracao inicial e operacao diaria |

## Ultima validacao objetiva registrada

- `dotnet build PrimoAutoEletrica.sln`
- `dotnet run --no-build --project PrimoAutoEletrica\PrimoAutoEletrica.csproj -- --smoke-test`
- resultado UI: `128 checks aprovados`, `0 falhas`
- relatorio UI: `PrimoAutoEletrica/bin/Debug/net9.0-windows/Logs/smoke-tests/ui-smoke-2026-05-25-16-50-52.txt`
- `dotnet run --no-build --project PrimoAutoEletrica\PrimoAutoEletrica.csproj -- --workflow-test`
- resultado operacional: `42 checks aprovados`, `0 falhas` (incluindo 4 novos testes de backup profissional)
- relatorio operacional: `PrimoAutoEletrica/bin/Debug/net9.0-windows/Logs/workflow-tests/workflow-test-2026-05-27-19-XX-XX.txt`
