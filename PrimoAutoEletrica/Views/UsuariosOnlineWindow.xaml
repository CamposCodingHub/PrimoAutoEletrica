<Window x:Class="PrimoAutoEletrica.Views.UsuariosOnlineWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Usuarios Online - Primo Auto Eletrica"
        MinWidth="900"
        MinHeight="600"
       
       
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanMinimize"
        Background="{DynamicResource AppBackgroundBrush}">

    <Window.Resources>
        <Style TargetType="TextBox" BasedOn="{StaticResource PremiumTextBox}"/>
    </Window.Resources>

    <Border Style="{StaticResource ModalSurface}"
            Padding="0"
            CornerRadius="18">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0"
                    Style="{StaticResource ModalHeaderSurface}"
                    CornerRadius="18,18,0,0">
                <DockPanel LastChildFill="False">
                    <StackPanel DockPanel.Dock="Left">
                        <TextBlock Text="Usuarios Online"
                                   Style="{StaticResource ModalHeaderText}"/>
                        <TextBlock Text="Visualize e gerencie as sessoes de usuarios ativas no sistema."
                                   Style="{StaticResource ModalSubheaderText}"
                                   Margin="0,8,0,0"/>
                    </StackPanel>

                    <Button DockPanel.Dock="Right"
                            Style="{StaticResource ModalCloseButton}"
                            Click="FecharButton_Click">
                        X
                    </Button>
                </DockPanel>
            </Border>

            <Grid Grid.Row="1"
                  Margin="24">
                <Border Style="{StaticResource ModalSectionCard}">
                    <StackPanel>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0"
                                       Text="Sessoes ativas"
                                       Style="{StaticResource ModalSectionTitle}"
                                       FontSize="20"/>

                            <Button Grid.Column="1"
                                    Width="150"
                                    Style="{StaticResource ModalSecondaryButton}"
                                    Click="AtualizarButton_Click"
                                    Margin="12,0,0,0" Content="{Binding Source={StaticResource LocalizationHelper}, Path=[Refresh]}"/>
                        </Grid>

                        <DataGrid x:Name="UsuariosDataGrid"
                                  AutoGenerateColumns="False"
                                  IsReadOnly="True"
                                  CanUserAddRows="False"
                                  CanUserDeleteRows="False"
                                  SelectionMode="Single"
                                  Margin="0,16,0,0"
                                  Height="350">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="{Binding Source={StaticResource LocalizationHelper}, Path=[Name]}" Binding="{Binding NomeUsuario}" Width="*"/>
                                <DataGridTextColumn Header="Perfil" Binding="{Binding Perfil}" Width="100"/>
                                <DataGridTextColumn Header="Maquina" Binding="{Binding MachineName}" Width="150"/>
                                <DataGridTextColumn Header="Login em" Binding="{Binding LoginAt, StringFormat=dd/MM/yyyy HH:mm}" Width="120"/>
                                <DataGridTextColumn Header="Ultima atividade" Binding="{Binding LastSeenAt, StringFormat=dd/MM/yyyy HH:mm}" Width="120"/>
                                <DataGridTextColumn Header="App" Binding="{Binding AppVersion}" Width="80"/>
                            </DataGrid.Columns>
                        </DataGrid>

                        <TextBlock x:Name="StatusTextBlock"
                                   Style="{StaticResource ModalSubheaderText}"
                                   Margin="0,12,0,0"
                                   TextWrapping="Wrap"/>
                    </StackPanel>
                </Border>
            </Grid>

            <Border Grid.Row="2"
                    Style="{StaticResource ModalFooterSurface}"
                    CornerRadius="0,0,18,18">
                <StackPanel Orientation="Horizontal"
                            HorizontalAlignment="Right"
                            Margin="24,16">
                    <Button Width="150"
                            Style="{StaticResource ModalSecondaryButton}"
                            Click="LimparExpiradasButton_Click"
                            Margin="0,0,12,0">
                        Limpar expiradas
                    </Button>
                    <Button Width="120"
                            Style="{StaticResource ModalAccentButton}"
                            Click="FecharButton_Click" Content="{Binding Source={StaticResource LocalizationHelper}, Path=[Close]}"/>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>



